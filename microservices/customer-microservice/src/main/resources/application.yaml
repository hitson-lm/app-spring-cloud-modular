spring:
  application:
    name: customer-microservice

  # ConfigServer centralizado
  config:
    import: ${SPRING_CONFIG_IMPORT:optional:configserver:http://localhost:8888}
    # import: "optional:configserver:http://localhost:8888" # para local
